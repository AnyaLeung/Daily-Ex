#include <stdio.h>

int main(void){
    double limit = 0;
    double input = 0;
    double value = 0;
    int flag = 0; //flag 0 for 0, 2, 4
    int count = 1;
    int divide = 0;

    scanf("%f %f", limit, x);

    while(value>=limit){
        if(flag==0){
            for(int i=1; i<=count; i++){
                x *= x;
            }
            value += x
        }
    }

    return 0;
}
